{
  "name": "draw-and-guess-multiplayer",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "build:platform": "npm --prefix client run build:platform",
    "build:platform:zips": "npm --prefix client run build:platform:zips",
    "test": "npm --prefix server run test"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
