# Update Word Bank Implementation Plan (No AI)

根据用户反馈，我们移除了 AI 相关的功能，仅专注于标准词库的实现和验证。

## 1. 任务清理与调整
- 移除 `tasks.md` 中关于 AI 生成和 OpenAI 集成的所有任务。
- 确认 `Task 1` (标准词库实现) 已完成。
- 调整剩余任务为验证和清理。

## 2. 验证 (Verification)
- **标准词库验证**: 确保游戏启动时正确加载 `server/words.json`。
- **游戏流程验证**:
  - 启动服务器。
  - 模拟用户加入房间。
  - 确认游戏开始后，选词逻辑正确使用了新词库中的单词。
  - 确认单词分类功能（如果有实现接口）正常工作，或者确认随机选词覆盖了所有分类。

## 3. 代码清理 (如果有残留)
- 检查 `server/index.js` 是否有为了 AI 预留的未使用代码。
- 确保 `package.json` 没有引入不必要的 AI 依赖（如 `openai`）。

## 下一步行动
执行验证，确保标准词库功能稳定，然后结束任务。
