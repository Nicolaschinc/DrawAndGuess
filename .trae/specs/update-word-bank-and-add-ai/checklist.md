- [x] Checkpoint 1: Standard Word Bank
  - Verify `server/words.json` contains valid JSON structure with categories.
  - Verify `server/wordManager.js` can load words from `server/words.json`.
  - Verify `server/index.js` uses `WordManager` to pick words instead of hardcoded `WORDS` array.

- [x] Checkpoint 2: Verification
  - Verify that the game starts and allows users to join rooms.
  - Verify that users can play a round with words from the standard bank.
