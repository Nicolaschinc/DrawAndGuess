{
  "name": "draw-and-guess-client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "build:client": "vite build",
    "build:ssr": "vite build --ssr src/entry-server.jsx --outDir dist-ssr",
    "prerender": "node scripts/prerender.mjs",
    "build": "npm run build:client && npm run build:ssr && npm run prerender",
    "build:platform": "VITE_ROUTER_BASE=/ vite build --base=./ --outDir ../releases/platform",
    "build:platform:zip": "npm run build:platform && (cd ../releases && rm -f drawguess-platform.zip && zip -r drawguess-platform.zip platform)",
    "build:platform:zips": "npm run build:platform && (cd ../releases && rm -f drawguess-itchio.zip drawguess-kongregate.zip drawguess-crazygames.zip && zip -r drawguess-itchio.zip platform && zip -r drawguess-kongregate.zip platform && zip -r drawguess-crazygames.zip platform)",
    "preview": "vite preview"
  },
  "dependencies": {
    "crypto-js": "^4.2.0",
    "i18next": "^25.8.13",
    "i18next-browser-languagedetector": "^8.2.1",
    "lucide-react": "^0.575.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-helmet-async": "^2.0.5",
    "react-i18next": "^16.5.4",
    "react-router-dom": "^7.13.1",
    "socket.io-client": "^4.7.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "sass": "^1.93.2",
    "sharp": "^0.34.5",
    "vite": "^5.4.10"
  }
}
